{
  "name": "seraph_model",
  "version": "0.0.0",
  "description": "thin model layer for seraph/neo4j",
  "main": "model.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "BSD"
}
